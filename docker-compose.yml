services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: workfit-web
    restart: unless-stopped
    ports:
      - "5173:5173"
    environment:
      # Proxy handles API calls, but we can default this to empty
      VITE_API_URL: ""
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      api:
        condition: service_started

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: workfit-api
    restart: unless-stopped
    environment:
      API_PORT: 3001
      FRONTEND_URL: http://localhost:5173
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: workfit
      DB_USER: workfit
      DB_PASSWORD: workfit
    ports:
      - "3001:3001"
    volumes:
      - ./server:/app
      - /app/node_modules
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    container_name: workfit-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: workfit
      POSTGRES_USER: workfit
      POSTGRES_PASSWORD: workfit
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U workfit -d workfit"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
